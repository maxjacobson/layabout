.video{ video_class_for(bookmark) }
  %h3.title{data: { url: bookmark.url }}= bookmark.title
  - if bookmark.watchable?
    %p this is a video
  %p= bookmark.description
  %p
    %code#url= link_to bookmark.url, bookmark.url
  = render 'bookmarks/meta', bid: bookmark.bid, liked: bookmark.liked?
